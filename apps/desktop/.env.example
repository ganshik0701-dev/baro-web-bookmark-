# apps/desktop 전용. electron-vite는 이 폴더의 .env만 읽는다.
# 여기 값은 빌드할 때 설치 파일에 그대로 들어간다. 공개해도 되는 값만 둔다.
# 서비스 키, DB 주소 같은 비밀값은 apps/api/.env에 둔다.

VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_SUPABASE_URL=https://xxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
