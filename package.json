{
  "name": "baro",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.34.5",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev:desktop": "pnpm --filter @baro/desktop dev",
    "dev:api": "pnpm --filter @baro/api dev",
    "build:desktop": "pnpm --filter @baro/desktop build",
    "build:api": "pnpm --filter @baro/api build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
